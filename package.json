{
    "name": "lambda-concept-x-ray",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "aws-xray-sdk-core": "^3.3.4",
        "aws-sdk": "^2.1048.0"
    },
    "devDependencies": {
        "jest": "latest"
    },
    "main": "index.js",
    "scripts": {
        "test": "jest",
        "configure": "exec ./scripts/configure",
        "deploy": "exec ./scripts/deploy",
        "s3-bucket": "exec ./scripts/s3-bucket",
        "delete-s3-bucket": "exec ./scripts/delete-s3-bucket",
        "build-layer": "exec ./scripts/build-layer",
        "invoke": "exec ./scripts/invoke",
        "clean": "exec ./scripts/clean"
    }
}
